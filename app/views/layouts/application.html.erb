<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="bg-white d-flex justify-content-between px-5 pt-3">
      <%= link_to lists_path, class: "text-muted text-decoration-none" do %>
        <p><strong>My Lists</strong></p>
      <% end %>
      <%= link_to new_list_path, class: "text-muted text-decoration-none"  do %>
        <p><strong>New List</strong></p>
      <% end %>
    </nav>

    <div class="container px-5 pt-3 pb-5">
      <div class="row">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
